#include <stdio.h>

#include "c_study.h"

/*
 * C 语言使用 signed 和 unsigned 关键字来修饰 字符型 和 整型 ，即：声明有符号类型（默认）和无符号类型。
 * 对于 signed 关键字而言，表示一个类型带有正负号，可以表示正负整数和 0 ，如：int 类型。
 * 对于 unsigned 关键字而言，表示一个类型不带有正负号，只能表示正整数和 0 ，如：unsigned int 类型。
 *
 * 对于 int 类型，默认是带有正负号的，即：int =  signed int 。
 *
 * 在生活中，有些数据是没有正负号的，如：年龄、体重、身高等，这种数据就适合使用 unsigned 关键字来修饰。
 * 在生活中，有些数据是有正负号的，如：温度、距离、速度等，这种数据就适合使用 signed 关键字来修饰。
 */
int main() {
    return 0;
}